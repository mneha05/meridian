# Optional. Without it, natural-language querying uses a deterministic local
# parser that handles the built-in question library. With it, free-form
# natural-language questions are translated to SQL by the model.
ANTHROPIC_API_KEY=
